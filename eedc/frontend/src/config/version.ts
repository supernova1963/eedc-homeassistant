/**
 * Zentrale Versionskonfiguration
 * Hier wird die Version nur einmal definiert und überall verwendet
 */

export const APP_VERSION = '0.9.7'
export const APP_NAME = 'eedc'
export const APP_FULL_NAME = 'Energie Effizienz Data Center'

// Formatierte Strings für die Anzeige
export const VERSION_STRING = `${APP_NAME} v${APP_VERSION}`
export const FULL_VERSION_STRING = `${APP_NAME} v${APP_VERSION} – ${APP_FULL_NAME}`
