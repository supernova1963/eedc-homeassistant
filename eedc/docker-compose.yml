services:
  eedc:
    image: ghcr.io/supernova1963/eedc:latest
    # Alternativ lokal bauen: docker compose build
    # build: .
    ports:
      - "8099:8099"
    volumes:
      - eedc-data:/data
    environment:
      - TZ=Europe/Berlin
    restart: unless-stopped

volumes:
  eedc-data:
