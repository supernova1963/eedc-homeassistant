# Home Assistant Add-on Konfiguration
# https://developers.home-assistant.io/docs/add-ons/configuration

name: "eedc - Energie Effizienz Data Center"
description: "Lokale PV-Anlagen Auswertung und Wirtschaftlichkeitsanalyse"
version: "4.0.25"
slug: "eedc"
url: "https://github.com/supernova1963/eedc-homeassistant"

# Breaking Change Warnung
# 2.0.0: Neuinstallation wegen geändertem Volume-Mapping (config:ro)
# 4.0.0: komplett neue Oberfläche (Cockpit · Komponenten · Auswertungen) —
#        keine Daten betroffen, alte Links werden umgeleitet; Hinweis vor dem Update.
breaking_versions:
  - "2.0.0"
  - "4.0.0"

# Unterstützte Architekturen
arch:
  - aarch64
  - amd64

# Container-Einstellungen
init: false
startup: application
boot: auto

# Home Assistant Integration
homeassistant_api: true
hassio_api: true

# Ingress (nahtlose UI-Integration)
ingress: true
ingress_port: 8099
ingress_stream: true
panel_icon: "mdi:solar-power"
panel_title: "eedc"
# Ohne diese Zeile behandelt Home Assistant den Seitenleisten-Eintrag als
# Administrator-Sache (Vorgabe `panel_admin: true`) — eedc fehlte damit bei jedem
# Benutzer ohne Administratorrechte, etwa auf Wandtablets (#393, jedema1805). Home
# Assistant sieht diesen Weg ausdrücklich vor; im Quelltext steht dazu wörtlich
# „Endpoints needed for ingress can't require admin because add-ons can set
# `panel_admin: false`". Wer verhindern möchte, dass Mitbewohner etwas verstellen,
# schaltet in eedc unter *Einstellungen → Anlage* die Einstellungs-PIN ein (#391).
panel_admin: false

# Benutzer-Optionen
options:
  log_level: info
  einstellungen_pin_zuruecksetzen: false
  ha_recorder_db_url: ""
  live_snapshot_5min_enabled: false
  mqtt:
    enabled: false
    host: "core-mosquitto"
    port: 1883
    username: ""
    password: ""
    auto_publish: false
    publish_interval_minutes: 60

schema:
  log_level: list(debug|info|warning|error)
  # Rückweg für eine vergessene Einstellungs-PIN. Einschalten, eedc neu starten,
  # danach wieder ausschalten — sonst ist die PIN nach jedem Start weg. Bewusst
  # hier und nicht als Adresse in der Oberfläche: Der Rückweg soll Zugriff auf die
  # Add-on-Konfiguration verlangen, den Mitbewohner nicht haben.
  einstellungen_pin_zuruecksetzen: bool?
  # Deprecated (Phase 5 #303): reportlab entfernt, Wert wird ignoriert. Bleibt
  # im Schema, damit bestehende Konfigurationen mit gesetzter Option die
  # Options-Validierung beim Add-on-Update nicht brechen.
  pdf_engine: list(reportlab|weasyprint)?
  ha_recorder_db_url: str?
  live_snapshot_5min_enabled: bool?
  mqtt:
    enabled: bool?
    host: str?
    port: int(1,65535)?
    username: str?
    password: password?
    auto_publish: bool?
    publish_interval_minutes: int(5,1440)?

# Persistenter Speicher
map:
  - data:rw
  - config:ro  # Lesezugriff auf HA-Config für statistics Datenbank-Abfrage

# Hot-Backup-Konsistenz: WAL ins Hauptfile zurückspielen + truncaten, bevor
# der Supervisor /data tarballt. App bleibt online, MQTT-Inbound läuft weiter.
backup_pre: "sqlite3 /data/eedc.db 'PRAGMA wal_checkpoint(TRUNCATE);'"

# Netzwerk (nur intern via Ingress)
# Port 8099 ist standardmäßig deaktiviert (null) — eedc läuft sicher über
# Ingress. Nur freigeben, wer die eedc-Werte per REST-Sensor (rest:-Plattform
# in der configuration.yaml) nach HA holen will; MQTT braucht keine Freigabe.
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "Nur für den REST-Export der eedc-Sensoren nach Home Assistant freigeben (z. B. 8099). Standardmäßig aus — für die normale Nutzung und den MQTT-Export nicht nötig."

# Vorgebautes Image von GitHub Container Registry
image: "ghcr.io/supernova1963/eedc-homeassistant-{arch}"
