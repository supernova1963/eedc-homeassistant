# ADR-002 — Sechs Wurzelmuster für Daten-, Zugriffs- und Aggregations-Invarianten

**Status:** Akzeptiert (2026-07-26)
**Auslöser:** Die v4.0.1-Runde heilte 19 Fundstellen einzeln — und **jeder Fix legte den nächsten frei**, weil dieselbe Ursache an mehreren Stellen unabhängig kopiert war. Der Sweep dahinter (`docs/drafts/BEFUND-SWEEP-WURZELMUSTER.md`) zählte nicht Symptome, sondern Ursachen: **sechs Muster, 44 Fundstellen**. Diese ADR schreibt die Muster als Regeln fest, damit sie nicht neu entstehen.

**Abgrenzung zu den anderen SoT-Regimen (bewusst, nicht kosmetisch):**

| Dokument | Regelt |
| --- | --- |
| `docs/KONZEPT-STYLE-GUIDE.md` (Regel 0/0a) | **Darstellung** — Farben, Komponenten, Typografie, Chart-Konventionen |
| `docs/ADR-001-BERECHNUNGS-LAYER.md` | **Schichtung** — wo eine Aggregat-Formel definiert wird (`core/berechnungen/`) |
| **ADR-002 (dieses Dokument)** | **Invarianten** — welche Aussage ein Wert überhaupt tragen darf, und woher er kommen muss |

P1–P6 sind keine Darstellungsregeln und gehören deshalb **nicht** in den Style-Guide. Sie sind auch keine Schichtungsregel: ADR-001 sagt, *wo* eine Formel steht; ADR-002 sagt, *was* sie behaupten darf.

## Form-Präzedenz: ADR + pytest-Konformitätswächter

ADR-001 hat seit Juni 2026 mit `backend/tests/test_berechnungs_layer_konformitaet.py` ein maschinelles Gegenstück. ADR-002 hat es ebenfalls — `test_wurzelmuster_konformitaet.py` plus vier Regressions-/Symmetrie-Dateien. **Das ist dasselbe Paar, kein neues Muster.**

Ein Unterschied ist erwähnenswert, aber ausdrücklich **kein** Vereinheitlichungsbedarf: der ADR-001-Wächter arbeitet per **Grep + Whitelist-Datei**, die ADR-002-Wächter per **AST**. Das Werkzeug folgt dem Muster, nicht umgekehrt — ADR-001 sucht Textmuster (`k.startswith("pv_")`), ADR-002 sucht Aufrufformen, die mehrzeilig und über Aliase laufen (`(imd.verbrauch_daten or {}).get(KEY)`). Ein Regex fände sie nicht.

**Backend-Wächter sind pytest, keine `check:*`-Skripte.** Alle 23 `check:*` sind Frontend-Node-Skripte (`eedc/frontend/scripts/check-*.mjs`). Ein Backend-Grep-Skript daneben wäre eine zweite Mechanik für dieselbe Aufgabe; als pytest läuft der Wächter im bestehenden Gate mit und die Baseline steht als Konstante im Code statt in einer separaten Allowlist-Datei.

---

## Die sechs Regeln

Die Spalte **„gesichert durch"** ist Pflicht und trägt den **ehrlichen Stand von heute**. Ohne sie ist eine ADR eine Wunschliste; mit ihr ist sie eine Landkarte, aus der man den nächsten Schritt abliest.

**Zwei Zusicherungen, die nicht dasselbe sind.** Ihre Vermischung ist genau die Überzeichnung, gegen die diese Spalte gebaut wurde — deshalb steht die Art der Deckung ab A22 als **eigene Spalte** daneben:

| Deckung | Was sie leistet | Was sie **nicht** leistet |
| --- | --- | --- |
| **Wächter** | prüft den **ganzen Baum** (AST über alle Produktiv-Python-Dateien) und schlägt auch an einer Stelle an, **die es heute noch nicht gibt**. Baseline klassifiziert, Gegenprobe gegen stilles Grünwerden. | sagt nichts über Werte — nur über Form. |
| **Regression** | prüft **die behobenen Stellen** an ihrem Verhalten, oft über mehrere Datenkonstellationen. | **was der Test nicht namentlich aufruft, sieht er nicht.** Ein neuer Endpoint mit demselben Fehler läuft durch keinen dieser Tests. |
| **ungedeckt** | — | Teil einer Regel ohne maschinelle Deckung; steht in §„Was noch nicht gewächtert ist". |

**Diese ADR hat die Unterscheidung im ersten Anlauf selbst verletzt** (Korrektur A22): für **P3** stand hier der AST-Wächter aus `04ac50bc` — der bewacht aber `_distribute_by_param`, also die **Umgehung des Zuordnungs-SoT** (#352), und **nicht** die Kennwert-Leseregel des P3-Satzes (die #229-Klasse: Attributzugriff `.leistung_kwp` statt `get_pv_kwp`). Auch P6 greift dort nicht: der prüft JSON-**Schlüssel**, und ein Attributzugriff ist kein Schlüssel. **Belegt hat die Lücke N66** — eine Falschmeldung „PV-Module kWp stimmt nicht mit Anlagenleistung überein" fiel zwischen die Pakete C und D, weil kein Gate sie fangen konnte (behoben in `c9a021c9`, ohne Wächter). P3 ist unten deshalb in seine drei prüfbaren Zusicherungen zerlegt; die sechs abgenommenen Regelsätze bleiben im Wortlaut unverändert.

| # | Regel | Deckung | gesichert durch (Stand 2026-07-27, nach A24-3) |
|---|---|---|---|
| **P1** | **Ein Anlagen-Kennwert darf nie aus EINER einzelnen Investition abgeleitet werden** — außer der Aufrufer hat vorher bewiesen, dass alle Investitionen in diesem Kennwert übereinstimmen (Guard im Code, nicht im Kommentar). | **Regression** | `tests/test_wurzelmuster_p1_orientierung.py` — **8 Tests**, die je einen **namentlich aufgerufenen** Pfad durch eine ausgeglichene Ost/West-Fixture schicken und zählen, wie viele Orientierungen dort ankommen: `get_tagesprognose` (Kanon · Fallback · Teilsummen-Meldung), `get_solar_prognose_endpoint`, `get_live_wetter`, `_get_pv_orientierungsgruppen`, `_lade_anlage_mit_pv`, `live_wetter_cache_key`. **Deckt genau diese Einsprungpunkte** — ein neuer Pfad, der einen Anlagen-Kennwert aus `invs[0]` bildet, läuft durch keinen dieser Tests. Ein Grep-Wächter ist **nicht möglich** (s. u.), ein Wert-Wächter wurde nie versucht. |
| **P2** | **Der kWp-Anteil ist ein Prognose-Schlüssel, kein Ertragsschlüssel.** Auf der IST-Seite verteilt er nur, wenn kein Messwert existiert — und dann **gekennzeichnet**. | **Regression** | `tests/test_wurzelmuster_p2_symmetrie.py` — Σ Pro-Modul == Σ Anlagen-Summe über **5 Konstellationen** (nur Aggregat · alle gemessen · Messwerte schlagen Aggregat · Teil-Lücke **mit** Aggregat · gar keine Quelle), **Baseline 0**. **Die Breite liegt in den Daten, nicht im Code:** geprüft werden genau **zwei** Sichten (`get_pv_strings_gesamtlaufzeit`, `list_monatsdaten_aggregiert`). Rechnet eine dieser zwei wieder selbst statt `resolve_pv_je_modul` zu lesen, schlägt der Test an — eine **dritte** Sicht, die morgen nach kWp verteilt, sieht er nicht. Helper-Verhalten: `test_pv_verteilung_helper.py`, `test_aggregiert_kwp_verteilung.py`, `test_pv_strings_kwp_verteilung.py`. |
| **P2-A** | **Benannte Ausnahme N42 — Teil der Regel, nicht ihr Gegner.** Bei einer **Teil-Lücke ohne Aggregat** (ein Modul gemessen, das andere nicht, kein Anlagen-Gesamtwert) behält die **Pro-Modul-Sicht** ihre Messwerte, während die **Anlagen-Summe bewusst nichts zeigt**: eine Teilsumme als „Gesamt-PV" auszuweisen wäre irreführend, einen Messwert wegzuwerfen wäre Datenverlust. **`Σ pv_strings ≠ Σ /aggregiert` ist dort gewollt und kein Aggregations-Drift.** | **Regression** + Prosa-Pflicht | Festgeschrieben in `04ac50bc`: Docstring an `core/berechnungen/pv_verteilung.py::resolve_pv_je_modul` + am Test `test_pv_strings_kwp_verteilung.py::test_teilluecke_ohne_aggregat_behaelt_messwert`. Der Symmetrie-Wächter nimmt den Fall über `_N42_TEILLUECKE` **ausdrücklich aus** und prüft ihn stattdessen auf seine eigene, asymmetrische Erwartung (`test_n42_teilluecke_ohne_aggregat_ist_bewusst_asymmetrisch`). Die Ausnahme ist damit **sichtbar geprüft, nicht stumm ausgelassen**. **Maschinell nicht gesichert** ist die Folgepflicht: dass ein *künftiger* Symmetrie-Wächter den Fall ebenso ausnimmt, steht nur als Pflicht Nr. 4 in Prosa — **jeder künftige Symmetrie-Wächter muss den Fall ausnehmen**, sonst „repariert" der nächste Durchgang die Regel weg. |
| **P3** | **Investitions-Kennwerte werden ausschließlich über den SoT-Helper gelesen** (`get_inv_value` / `get_pv_kwp` / `get_pv_neigung` / `get_pv_azimut`). Ein Literal-Schlüssel im `parameter`-JSON ist nur gültig, wenn er im Kanon `core/investition_parameter.py` bzw. `lib/investitionParameter.ts` steht. | *zerlegt* → **P3-a · P3-b · P3-c** | Der Regelsatz bleibt wie abgenommen. Er trägt aber **zwei** Zusicherungen, und der vorhandene Wächter deckt **keine von beiden** — er deckt eine dritte. Deshalb aufgeschlüsselt: |
| **P3-a** | *Erster Halbsatz:* Kennwerte nur über den SoT-Helper lesen — nie den Attributzugriff `.leistung_kwp` / `.neigung_grad` direkt auf einer Investition (die **#229-Klasse**). | **Wächter** (nur `leistung_kwp`) | AST-Wächter aus A24-3: `test_wurzelmuster_konformitaet.py::test_p3a_investitions_kwp_nur_ueber_den_sot_helper` — **baumweit**, prüft `ast.Attribute` mit `ast.Load`, **plus** `::test_p3a_investitions_kwp_nicht_per_getattr_umgehen` für die Form `getattr(inv, "leistung_kwp")`. Der zweite Zweig ist **nicht optional**: `co2_amortisation.py:79` las genau so und tauchte deshalb in *keiner* Erhebungszahl auf — ohne ihn wäre der Wächter grün geblieben, während eine Stelle unmigriert war. Empfänger-Heuristik statt Typinferenz: `P3A_ERLAUBTE_EMPFAENGER = {anlage, Investition}` (die Anlage hat kein `parameter`-JSON, es gibt dort keinen abweichenden effektiven Wert; `Investition` hält die heute leere Spaltenausdruck-Kategorie offen). Der siebte Allowlist-Eintrag (`crud.py::self`, A26) ist die Bestätigung von Grenze (a) am eigenen Code: `InvestitionResponse.leistung_kwp_effektiv` **erzeugt** den Kennwert und muss die Rohspalte dafür lesen. **Baseline 0 nach A24-2** — 47 Zugriffe im Baum, 40 auf `anlage`, 7 klassifiziert in `P3A_BASELINE_AUSNAHMEN` (Modell-`__repr__` · `PvModul`-Dataclass · `PVModulPrognose`-Pydantic · Export-Rohspalten-Spiegelung · zwei typgeschützte PDF-Rohzweige, weil dieselbe Spalte beim Speicher kWh und beim Wechselrichter kW AC trägt) + der SoT selbst für den `getattr`-Zweig; `::test_p3a_baseline_ausnahmen_sind_noch_belegt` verhindert eine verwaiste Ausnahme. Gegenprobe `::test_p3a_kennwert_sot_traegt_die_regel_noch` prüft im Quelltext, dass `get_pv_kwp` **beide** Stufen (Spalte → `KWP_PARAM_KEYS` aus dem Kanon) und `get_bkw_kwp` alle vier (inkl. `leistung_wp × anzahl` und `ANZAHL_LESE_DEFAULT`) noch trägt und `get_erzeuger_kwp` noch dispatcht. **Drei Grenzen, alle am Code gemessen und keine Fußnote:** (a) **kein Typwissen** — ein Empfänger, der `anlage` heißt, aber eine Investition hält, ist falsch-negativ per Konstruktion (dieselbe bewusste Grenze wie die Modellnamen-Liste bei P5); umgekehrt ist jedes neue Response-Schema mit einem `leistung_kwp`-Feld ein falsch-**positiv** und braucht einen Allowlist-Eintrag. (b) **Dict-Zugriffe** (`inv_dict["leistung_kwp"]`, `row.get("leistung_kwp")`) sind nicht erfasst — das ist **P6-Territorium** (Literal-Schlüssel gegen Schlüssel-SoT), nur auf einem anderen Feld. (c) **Das FRONTEND deckt ein zweiter Wächter** — dieser hier endet an der API-Grenze. Bis A26 rechnete `v4/komponentenAdapter.tsx:260,288` dieselbe kWp-Verteilung ein zweites Mal auf der Rohspalte; ein Backend-pytest sagt darüber nichts (N106). Seit A26 liefert `InvestitionResponse` zusätzlich `leistung_kwp_effektiv` (nur lesend, **nicht** in `InvestitionBase` — sonst nähme der Schreibpfad es entgegen), die Anzeige-/Rechen-Stellen lesen es, und `frontend/scripts/check-kennwert-roh.mjs` (`npm run check:kennwert-roh`, CI) hält die Trennlinie: **Anzeige/Rechnung effektiv, Formulare/Wizards roh**. Mechanik bewusst dieselbe — keine Typinferenz, sondern Empfänger-Namensheuristik plus `Datei::Empfänger`-Allowlist mit Klartext-Begründung; **Baseline 0**, 42 Zugriffe geprüft, 11 klassifiziert (2 Eingabe, 9 gleichnamiges Feld auf einer anderen Response), 3 baumweit erlaubte Empfänger (`anlage`, `formData`, `Investition`). Seine eigenen Grenzen stehen im Skriptkopf: nur die Attributform (kein `x['leistung_kwp']` — heute existiert keins), und nur `leistung_kwp`. Nur `leistung_kwp`; `neigung_grad`/`ausrichtung` bewusst nicht (s. §offen). Punktuelle Regression daneben unverändert: `test_inv_value_spalten_fallback.py`, `test_daten_checker_kwp_detailfeld.py` (die N66-Stelle). |
| **P3-b** | *Zweiter Halbsatz:* ein Literal-Schlüssel im `parameter`-JSON nur, wenn er im Kanon steht. | **ungedeckt** | **Kein Wächter** — aber seit A24-1 baubar und in A24-3 erstmals **gemessen** (Baseline + Zuschnitt in §offen): der Kanon `core/investition_parameter.py` trägt die PV-Leistung jetzt (`PARAM_PV_MODULE["LEISTUNG_KWP"]`, Legacy `kwp` in `LEGACY_PARAM_KEYS`), ein Wächter müsste dafür keine Ausnahme mehr eintragen. Vorher fehlte der Eintrag ganz. |
| **P3-c** | **Ein Gesamtwert wird nur über die Zuordnungs-SoT verteilt** — `_distribute_by_param` nie direkt, sondern über `_mapped_or_distribute`, das vorher die explizite Feld→Investition-Zuordnung prüft. *(Diese Zusicherung trägt der vorhandene Wächter; im P3-Satz stand sie nicht — A22 nachgetragen, damit Deckung und Anspruch demselben Satz zugeordnet sind.)* | **Wächter** | AST-Wächter aus `04ac50bc`: `test_wurzelmuster_konformitaet.py::test_p3_distribute_by_param_nur_ueber_die_zuordnungs_sot` — **baumweit** über alle Produktivdateien, erfasst Aufruf **und** Import (Alias-Umweg), **Baseline 0** (`N48_BASELINE_AUSNAHMEN` leer). Gegenprobe `::test_p3_zuordnungs_sot_existiert_noch` prüft, dass `_distribute_by_param` **und** `_mapped_or_distribute` in `api/routes/connector.py` noch definiert sind. Bewacht **#352** (`0a08cca6`) — **nicht** die #229-Klasse. |
| **P4** | **Ein Wert-Pfad darf nie eine Null oder eine Teilsumme als gültiges Ergebnis ausliefern, ohne dass die Antwort selbst es sagt.** Die Unvollständigkeit gehört in die **Response** (`hinweise`/Provenance), nicht ins Log. | **Regression** | Response-Vertrag + Anzeige seit A17 (`f1720cc0`), geprüft in `tests/test_wurzelmuster_p4_teilsumme.py` — **6 Tests am HTTP-Ergebnis von zwei Endpoints** (`solar_prognose::get_solar_prognose_endpoint`, `energie_profil.views::get_tagesprognose`): `vollstaendig=False ⇒ nichtleeres hinweise` inkl. Umfang („1 von 2"), der **Wert wird nicht ersetzt, nur beschriftet** (die 128,0 kWh bleiben die Teilsumme), vollständiger Fan-out bleibt still, 24 Nullen ohne Prognose sagen es, Solcast-Profil-von-heute wird als Näherung ausgewiesen. **Geprüft sind diese zwei Response-Verträge, nicht die Form `except → return 0`** — ein neuer stiller Pfad in einem dritten Endpoint fällt durch. Ein Grep-Wächter ist **nicht möglich** (s. u.). Referenzform: `live_wetter.py` (`verfuegbar` + `grund`). |
| **P5** | **Die aktive PVGIS-Prognose ist genau `ist_aktiv == True`, `ORDER BY abgerufen_am DESC`, `LIMIT 1`.** `ist_aktiv` ist **Nutzerwille** (eine ältere Prognose zu aktivieren ist erlaubt) — „die neueste" wäre stumme Übersteuerung, „irgendeine aktive" ein Zufallswert. | **Wächter + Regression + DB-Invariante** | Drei Ebenen, alle drei am Code gegengeprüft — die **stärkste Deckung der sechs**. (1) **Wächter:** `test_wurzelmuster_konformitaet.py::test_p5_aktive_prognose_nur_ueber_den_auswahl_sot`, baumweit, Baseline 0, mit **3 klassifizierten Flag-Setzern** in `P5_BASELINE_AUSNAHMEN` (`pvgis.py` Verwaltung · `json_operations.py` Export/Import-Normalisierung · `demo_data.py` Schreibpfad); Gegenprobe `::test_p5_auswahl_sot_traegt_die_regel_noch` prüft alle drei Regelbestandteile (`ist_aktiv.is_(True)`, `abgerufen_am.desc()`, `.limit(1)`) im Quelltext des Helpers. (2) **DB-Invariante:** partieller Unique-Index `INDEX_EINE_AKTIVE_PROGNOSE` — deklariert im Model (`models/pvgis_prognose.py:69`, `sqlite_where=ist_aktiv = 1`) **und** als Start-Migration für Bestands-DBs (`core/database.py:428`); die Invariante hängt also nicht an der Disziplin der Schreibpfade. (3) **Regression:** `tests/test_wurzelmuster_p5_invariante.py`, **13 Tests** (Index greift · erlaubt beliebig viele inaktive · ältere aktivieren funktioniert trotz Index · Bestandsbereinigung **deaktiviert statt löscht**, idempotent · Import normalisiert auf genau eine aktive, warnt dabei und respektiert eine bewusst aktivierte ältere · keine 500er in Daten-Checker/Social-Karte · kein doppelter SOLL im Monatsbericht). **Grenze des Wächters:** er erkennt das Modell über eine **feste Namensliste** (`PVGISPrognose`, `PVGISPrognoseModel`) und nur bei direktem Namen als Empfänger; ein *neuer* Import-Alias oder `models.PVGISPrognose.ist_aktiv` rutschte durch. Die drei heute existierenden Alias-Importe sind genau die drei Baseline-Module — der Bestand ist vollständig erfasst. SoT ist `backend/services/prognose_auswahl.py`, **alle 23 Lesestellen** darauf umgestellt (`c6bbb079`). |
| **P6** | **Ein Literal-Schlüssel auf einem JSON-Feld braucht eine Schlüssel-SoT.** Ein Zugriff, dessen Tippfehler still `0` liefert (und `0` sieht aus wie „keine Daten"), ist nur zulässig, wenn ein Wächter den Schlüssel gegen die SoT prüft. | **Wächter** — auf `.get()`-Lesezugriffe | AST-Wächter aus `04ac50bc`: `test_wurzelmuster_konformitaet.py::test_p6_verbrauch_daten_schluessel_stehen_in_der_feld_sot` gegen `core/field_definitions.py` — **baumweit**, erfasst auch `(imd.verbrauch_daten or {}).get(…)` und die lokale Variable, **Baseline 2, klassifiziert** (`sonderkosten_notiz` = Freitext, `sonstige_positionen` = Liste, beide keine Messfelder); `::test_p6_baseline_ausnahmen_sind_noch_belegt` verhindert, dass eine verwaiste Ausnahme später einen echten Treffer deckt. **Zwei Grenzen, beide am Code geprüft:** (a) nur das Feld `verbrauch_daten` — `parameter` und die PVGIS-`monatswerte` sind nicht erfasst (§offen); (b) nur die Form `…get("literal")`. Ein **Subscript** (`verbrauch_daten["x"]`) wird nicht geprüft — beim **Lesen** vertretbar (fehlender Schlüssel → `KeyError`, also laut statt still `0`), beim **Schreiben** nicht: 21 Zuweisungen der Form `verbrauch_daten["literal"] = …` sind ungeprüft, und ein Tippfehler dort erzeugt genau die stille `0` beim korrekt lesenden Aufrufer (§offen). |

**Als Landkarte gelesen (Stand nach A24-3):**

- **Wächter** — baumweit, fangen auch eine Stelle, die es heute nicht gibt: **P3-a** (seit A24-3, nur für `leistung_kwp`; Backend-Python **plus** seit A26 die Client-Hälfte `check:kennwert-roh`), **P3-c**, **P5**, **P6** (letzterer nur für `.get()`-Lesezugriffe auf `verbrauch_daten`).
- **Regression** — schützt die behobenen Stellen, sagt nichts über eine neue Stelle morgen: **P1** (6 Einsprungpunkte), **P2** (2 Sichten × 5 Datenkonstellationen), **P2-A**, **P4** (2 Response-Verträge) sowie die Verhaltensebene von **P5**.
- **ungedeckt**: **P3-b** (Wächter baubar, Baseline erhoben, nicht gebaut — s. §offen).

**Drei der sechs abgenommenen Regelsätze haben damit keinen Wächter.** Für P1 und P4 ist das eine begründete Entscheidung (nächster Abschnitt); für P2 ist es eine Frage der Reichweite (zwei Sichten sind erfasst, eine dritte wäre es nicht). Die P3-a-Lücke, die A22 als einzige echte Lücke benannt hatte, ist mit A24 geschlossen und mit A26 über die API-Grenze hinaus verlängert — **aber nur für `leistung_kwp`**; die Grenzen stehen in der Zeile selbst, nicht als Fußnote. Offen bleiben die in §„Was noch nicht gewächtert ist" genannten Punkte.

---

## Warum für P1 und P4 kein Grep-Wächter existiert

Das ist eine Entscheidung, keine Lücke.

- **P1:** `[0]` auf einer Investitions-Liste ist in geguardeten Fällen **korrekt** — `len(pv_module) == 1`, `len(unique_orientations) == 1`. Der Guard steht in einer **anderen Zeile** als der Zugriff. Ein Grep-Wächter behandelte die sauberen Fälle (`prefetch_service`, `solar_prognose`, `community_service`, `social.py` Ausrichtungs-Label) wie die Verstöße. Stattdessen: ein Regressionstest, der eine Multi-Orientierungs-Anlage durch **jeden** Pfad schickt und zählt, wie viele Orientierungsgruppen beim Wetter-Abruf ankommen.
- **P4:** `except → return 0/None` ist im **Connector-/Wetter-Layer die richtige** Form (~40 Stellen: Provider antwortet nicht → `None` → der Aufrufer schreibt gar nichts) und im **Wert-Pfad die falsche**. Der Unterschied liegt nicht im Ausdruck, sondern darin, ob der Aufrufer den Wert **ausliefert**. Stattdessen: ein Response-Vertrags-Test, der am HTTP-Ergebnis prüft, nicht im Log.

---

## Was noch nicht gewächtert ist (bewusst offen)

| Punkt | Warum offen | Voraussetzung |
| --- | --- | --- |
| **`neigung_grad` / `ausrichtung` im P3-a-Wächter** — die Attributnamen-Menge ist eine Konstante (`P3A_KENNWERT_ATTRIBUTE`), die Erweiterung wäre ein Listeneintrag | **Nicht die Heuristik ist das Problem, sondern die Helper.** `get_pv_neigung` defaultet auf 35°, `get_pv_azimut` auf Süd; beide können „fehlt" nicht von „gepflegt" unterscheiden. Eine blanke Migration der Anzeige-Stellen macht aus einem heute korrekt **leeren** Feld eine erfundene Zahl — `anlagendokumentation.py:122-128` druckt die Ausrichtungszeile heute nur, wenn sie gepflegt ist, und würde danach „Süd · 35° Neigung" für jedes ungepflegte Modul behaupten. Dazu ist `ausrichtung` ein über **vier** Objektarten geteilter Attributname (Investition, `Orientierungsgruppe`-Dataclass, PV-String, PVGISPrognose): die Allowlist trüge mehr Einträge als Treffer. | Eigene Erhebung mit eigener Vorfrage: brauchen die beiden Helper eine **default-freie** Variante für Anzeige-Stellen? Ohne die ist die Migration dort nicht sauber machbar. Erst danach der Listeneintrag. |
| **P3-b: `parameter`-Literal-Schlüssel gegen `investition_parameter.py`** | Kanon-Vorbedingung **erledigt** (A24-1). **Baseline erstmals erhoben (A24-3, AST):** **70 Literal-Zugriffe** auf ein `parameter`-JSON in 23 Dateien, daneben **74 Zugriffe in der gewünschten Konstanten-Form** — der Sollzustand ist also bereits die Mehrheit. Davon stehen **4 Schlüssel nicht im Kanon**, und die Klassifikation ist die eigentliche Erkenntnis: **3 sind falsch-positiv** (`alternativkosten.py:109,118,120` lesen `wp_agg[...]`, das Ergebnis von `_wp_aggregate(wp.parameter)` — ein abgeleitetes Dict, kein JSON), **1 ist echt** (`infothek_migration.py:201` `params.get("stamm_notizen")`). Die `stamm_*`-Familie hat eine **eigene, lokale Schlüssel-Map** im selben Modul — dieselbe Zweitliste-Klasse wie N115 unten. | Zwei Zuschnitt-Entscheidungen vor dem Bau: (1) **Alias-Verfolgung eng fassen** — nur `x = inv.parameter` / `x = inv.parameter or {}` als Alias werten, nicht „irgendwo `.parameter` im Zuweisungsausdruck"; die weite Form erzeugt genau die 3 falsch-positiven, und ein Wächter, der beim ersten Lauf zu 75 % danebenliegt, wird abgeschaltet. (2) **`stamm_*` entscheiden**: in den Kanon aufnehmen oder als eigene, benannte SoT freistellen — ein Allowlist-Eintrag pro Schlüssel wäre die falsche Antwort. Danach ist der Wächter klein (Baseline 0 oder 1). |
| **N115 — `LEGACY_PARAM_KEYS` und die v3.25.0-Migration hängen an nichts** | `LEGACY_PARAM_KEYS` (`core/investition_parameter.py:256`) ist **reine Dokumentation**; die Migration `core/database.py:64` (`KEY_MAPPING_BY_TYP`) führt ihre **eigene**, typgebundene Key-Liste. Nichts bindet die zwei aneinander — und sie **weichen bereits ab**: die Migration schreibt zusätzlich `ladeleistung_kw → max_ladeleistung_kw` (Wallbox, „community_service-Drift"), was in `LEGACY_PARAM_KEYS` **fehlt**. Umgekehrt trägt der Kanon `kwp`, den die Migration bewusst nie angefasst hat — das ist dort dokumentiert, die Wallbox-Lücke nicht. Eine Doku-Liste, die den ausgeführten Umbau unvollständig wiedergibt, ist genau die Sorte Behauptung, gegen die die Spalte „gesichert durch" gebaut wurde. | **Der eigentliche Wert liegt hier, nicht im Literal-Schlüssel-Wächter:** ein Test, der `LEGACY_PARAM_KEYS` gegen `KEY_MAPPING_BY_TYP` prüft (jedes Migrations-Paar steht im Kanon; jede Kanon-Abweichung ist benannt begründet). Klein, ohne Migration, ohne Baseline — sinnvollerweise **im selben Paket** wie P3-b. |
| Schreibzugriffe auf `verbrauch_daten` per Subscript (P6, 21 Stellen) | Der Wächter prüft `…get("literal")`, also den Lesepfad. Ein Tippfehler in `verbrauch_daten["typo_kwh"] = …` wirft nichts, sondern legt einen Schlüssel an, den kein Leser liest — der korrekte Leser bekommt still `0`. Dieselbe Klasse, andere Seite. | Den bestehenden P6-Wächter um `ast.Subscript` mit Literal-Slice erweitern (Lese- **und** Schreibform), Baseline erheben. Kein neuer Wächter, eine Erweiterung — die Schlüssel-SoT existiert bereits. |
| PVGIS-`monatswerte`-Schlüssel `e_m`/`h_m`/`sd_m`/`monat` (P6, 17 Literal-Stellen) | Es gibt **gar keine** Schlüssel-SoT — nur einen Kommentar am Model. Erst Konstanten bauen, dann wächtern. | `PVGIS_MW_*`-Konstanten (oder ein Pydantic-`PVGISMonatswert`) in `models/pvgis_prognose.py`. |
| Dead-Export-Wächter (jede öffentliche `async def` in `services/` braucht einen Importeur) | Der Wächter griffe weit über den Anlass hinaus; ohne vorher erhobene Baseline wäre er ein Aufräum-Paket, kein Wächter. | Baseline erheben (P0b-Dead-Code). |

### Die Skizze für P3-a ist abgearbeitet (A24) — was dabei anders kam als geplant

Die A22-Skizze („nicht gebaut, nur schneidbar gemacht") ist in drei Paketen abgeflossen: **A24-1** Kanon + Helper, **A24-2** Migration aller Read-Sites, **A24-3** Wächter. Die Reihenfolge war zwingend — eine Baseline in der erhobenen Größenordnung wäre kein Wächter gewesen, sondern ein Aufräum-Paket mit einem grünen Test obendrauf. Vier Punkte der Skizze haben am Code **nicht** gehalten; sie stehen hier, damit die nächste Erweiterung (Neigung/Azimut) sie nicht wiederholt:

1. **Die Allowlist-Kandidaten `services/pv_orientation.py` und `utils/investition_value.py` waren gar nicht nötig** — beide lesen per `getattr(inv, "leistung_kwp")`, also über einen **String**. Ein Attribut-Wächter sieht sie nicht. Genau daraus folgte die wichtigste Korrektur: der **`getattr`-Zweig ist nicht optional**. `co2_amortisation.py:79` las so und stand deshalb in *keiner* Erhebungszahl — der Wächter wäre grün gewesen, während eine Stelle unmigriert war.
2. **Die Kategorie „Spaltenausdruck in `select()/where()/order_by()`" ist leer** — kein einziger Zugriff hat `Investition` als Empfänger. Statt eines Allowlist-Eintrags steht der Name deshalb in `P3A_ERLAUBTE_EMPFAENGER` und hält die Kategorie für die Zukunft offen.
3. **Zwei Stellen waren nicht mechanisch migrierbar:** `Investition.leistung_kwp` ist ein **Mehrzweckfeld** — dieselbe Spalte trägt beim Speicher kWh und beim Wechselrichter kW (AC). Die beiden PDF-Builder haben deshalb je einen typgeschützten Rohspalten-Zweig behalten (Erzeuger über den Helper, Rest roh) und sind mit dieser Begründung in der Allowlist. Jede künftige Regel „`leistung_kwp` ist die PV-Nennleistung" ist am Code falsch, solange das so bleibt.
4. **Die Größenordnung lag am oberen Rand der Schätzung**, nicht darunter: 39 Migrationszeilen in 13 Modulen statt „grob 25–35 in ~11".

Was von der Skizze **trug**: die Entscheidung gegen Typinferenz. Der Wächter arbeitet über Empfänger-Namensheuristik plus `Modul::Empfänger`-Allowlist — dieselbe bewusste Grenze, die der P5-Wächter mit seiner Modellnamen-Liste zieht, und mit derselben Konsequenz: ein Empfänger, der `anlage` heißt, aber eine Investition hält, rutscht durch.

**`e_m` ist gefährlicher, als es aussieht:** dieselbe Größe heißt im gespeicherten JSON `e_m`, in der normalisierten Tabelle `PVGISMonatsprognose.ertrag_kwh` und im Export noch anders. Drei Namen über drei Schichten — die Voraussetzung dafür, dass der ursprüngliche Tippfehler-Bug (`e_month_kwh`) jahrelang unentdeckt blieb.

---

## Korrekturen gegenüber dem Sweep-Bericht

`docs/drafts/BEFUND-SWEEP-WURZELMUSTER.md` beschreibt den Stand **vor** den A17-/A20-Paketen. Fünf Stellen sind überholt; hier gilt die korrigierte Fassung, damit diese ADR nicht einen falschen Status zementiert:

1. **§1 „gesichert durch: nichts"** — gilt für **P1, P2, P4 und P5 nicht mehr.** Alle vier haben inzwischen Tests (Tabelle oben).
2. **§2, Wirkung von N52 und N53 war am Code falsch beschrieben.**
   - **N52** war *nicht* der Orientierungs-Kollaps: im Multi-String-Fall gehen Neigung und Azimut **gar nicht** an OpenMeteo. Der echte Fehler war eine **direkt aus der Spalte gelesene kWp** — ein Modul, dessen Nennleistung nur im `parameter`-JSON gepflegt war, fiel damit **ganz aus der Gruppierung und aus der Live-Gesamtleistung** (14,0 statt 10,0 kWp).
   - **N53** war eine **tote lokale Variable** und wurde **entfernt**, nicht korrigiert.
3. **§3.3 und die §9-Tabelle: N61 war schon vor A17 geschlossen** (`f06a993f`) — die Vorschlags-Beschriftung im Monatsabschluss trug bereits die ehrliche Herkunft.
4. **§4.1 „drei Konventionen"** gilt nicht mehr: `get_pv_kwp` liest inzwischen **beide** Legacy-Keys (`parameter["kwp"]` **und** `parameter["leistung_kwp"]`), damit `get_pv_kwp ⊇ get_inv_value("leistung_kwp")`. Wer eine kWp gepflegt hat, wird von beiden Wegen gefunden. Offen bleibt allein der fehlende **Kanon-Eintrag** (s. Tabelle oben).
5. **§8: die dort vorgeschlagenen Testdateinamen stimmen nicht mit den gebauten überein.** Verbindlich sind:

   | Muster | gebaute Datei |
   | --- | --- |
   | P3-a · P3-c · P5 · P6 (Struktur) | `backend/tests/test_wurzelmuster_konformitaet.py` |
   | P1 | `backend/tests/test_wurzelmuster_p1_orientierung.py` |
   | P2 | `backend/tests/test_wurzelmuster_p2_symmetrie.py` |
   | P4 | `backend/tests/test_wurzelmuster_p4_teilsumme.py` |
   | P5 (Verhalten) | `backend/tests/test_wurzelmuster_p5_invariante.py` |

---

## Pflicht ab heute

1. **Neuer Code, der einen Anlagen-Kennwert bildet, eine IST-Größe verteilt, einen Investitions-Kennwert liest, eine unvollständige Antwort ausliefert, die aktive Prognose wählt oder einen JSON-Literal-Key liest**, hält P1–P6 ein.
2. **Wer eine Baseline-Ausnahme hinzufügt, begründet sie im Klartext neben dem Eintrag** — warum die Stelle das Muster *setzt* statt es zu *verletzen*. Ein Lesepfad gehört nie in eine Baseline.
3. **Wer eine SoT umbenennt oder verschiebt, zieht den zugehörigen Gegenproben-Test mit.** Ohne ihn wird das Gate still grün und prüft ab da nichts mehr.
4. **Wer einen neuen Symmetrie-Wächter baut, nimmt N42 explizit aus** (P2-A).
5. **Wer die Spalte „gesichert durch" fortschreibt, trägt die Art der Deckung mit ein** — **Wächter** (baumweit, fängt auch eine neue Stelle) oder **Regression** (schützt die behobenen Stellen). Ein Regressionstest, der in der Spalte wie ein Wächter aussieht, ist genau die Überzeichnung, gegen die diese Spalte gebaut wurde; das ist dieser ADR im ersten Anlauf bei P3 selbst passiert (A22). Wer nicht entscheiden kann, was ein Test zusichert, trägt **offen** ein und schreibt dazu, was zur Entscheidung fehlt.

## Verbundene Dokumente

- `docs/ADR-001-BERECHNUNGS-LAYER.md` — Schichtentscheidung und ihr Konformitätswächter (Form-Präzedenz)
- `docs/KONZEPT-BERECHNUNGS-LAYER.md` — Submodul-Schnitt des Berechnungs-Layers
- `docs/drafts/BEFUND-SWEEP-WURZELMUSTER.md` — der Sweep, aus dem diese Regeln stammen (Stand vor A17/A20, s. Korrekturen oben)
- `docs/KONZEPT-STYLE-GUIDE.md` — das **andere** SoT-Regime (Darstellung); die zwei werden nicht vermischt

## Verbundene Memory-Einträge

- `feedback_aggregations_drift` — die Drift-Klasse, aus der P2 und P5 stammen
- `feedback_regelsaetze_eigene_adr` — warum Regelsätze aus Sweeps eine eigene ADR bekommen und nicht in den Style-Guide wandern
- `feedback_keine_regel_behaupten_ohne_code_beleg` — der Grund für die Pflicht-Spalte „gesichert durch"
